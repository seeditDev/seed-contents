{
  "questionId": "Q1875",
  "title": "Sort a Map by Values",
  "slug": "sort-a-map-by-values",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-01",
  "metadata": {
    "category": "Arrays",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "standard library & collections",
      "programming languages"
    ],
    "companyTags": [],
    "topicTags": [
      "Standard Library & Collections"
    ]
  },
  "content": {
    "problemStatement": "You’re working on a leaderboard feature for a quiz app. The scores of users are stored in a `HashMap&lt;String, Integer&gt;`, where the key is the username and the value is their score.\n\nYour task is to write a program that **sorts this map by values in descending order** (i.e., highest score first), and prints each key-value pair on a new line in the format:\n\n`username: score`\n\n\nInput Format\n\nFirst line: An integer `N`, the number of entries in the map.\nNext `N` lines: Each line contains a `username` and `score`, separated by a space.\n\n\nOutput Format\n\nPrint the sorted key-value pairs (by score descending), one per line, in the format:\n\n`username: score`",
    "constraints": [
      "1 &lt;= N &lt;= 1000",
      "`username` contains only lowercase letters and no spaces.",
      "0 &lt;= score &lt;= 10^6",
      "No duplicate usernames."
    ],
    "inputFormat": "",
    "outputFormat": "",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\nalice 250\nbob 450\ncarol 300\ndave 400",
        "output": "bob: 450\ndave: 400\ncarol: 300\nalice: 250",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "input": "4\nalice 250\nbob 450\ncarol 300\ndave 400",
        "expectedOutput": "bob: 450\ndave: 400\ncarol: 300\nalice: 250",
        "weight": 100
      }
    ],
    "totalTestCases": 1,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      100
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "",
    "timeComplexity": "",
    "spaceComplexity": "",
    "code": {
      "C++": "#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    int n, k;\n    cin >> n >> k;\n    \n    vector<pair<int, int>> pairs(n);\n    \n    for (int i = 0; i < n; ++i) {\n        cin >> pairs[i].first >> pairs[i].second;\n    }\n    \n    for (int i = 0; i < n; ++i) {\n        if ((pairs[i].first + pairs[i].second) % k == 0) {\n            cout << \"(\" << pairs[i].first << \", \" << pairs[i].second << \")\" << endl;\n        }\n    }\n    \n    return 0;\n}\n",
      "Python3": "def main():\n    n, k = map(int, input().split())\n    \n    pairs = []\n    \n    for _ in range(n):\n        first, second = map(int, input().split())\n        pairs.append((first, second))\n    \n    for first, second in pairs:\n        if (first + second) % k == 0:\n            print(f\"({first}, {second})\")\n\nif __name__ == \"__main__\":\n    main()\n",
      "C": "#include <stdio.h>\n\nint main() {\n    int n, k;\n    scanf(\"%d %d\", &n, &k);\n    \n    int pairs[n][2];\n    \n    for (int i = 0; i < n; ++i) {\n        scanf(\"%d %d\", &pairs[i][0], &pairs[i][1]);\n    }\n    \n    for (int i = 0; i < n; ++i) {\n        if ((pairs[i][0] + pairs[i][1]) % k == 0) {\n            printf(\"(%d, %d)\\n\", pairs[i][0], pairs[i][1]);\n        }\n    }\n    \n    return 0;\n}\n",
      "Java": "import java.util.*;\nimport java.lang.*;\nimport java.io.*;\n\nclass Codechef\n{\n    public static void main (String[] args) throws java.lang.Exception\n    {\n        Scanner sc = new Scanner(System.in);\n\n        int n = sc.nextInt();\n        HashMap<String, Integer> map = new HashMap<>();\n\n        for (int i = 0; i < n; i++) {\n            String username = sc.next();\n            int score = sc.nextInt();\n            map.put(username, score);\n        }\n\n        List<Map.Entry<String, Integer>> list = new ArrayList<>(map.entrySet());\n\n        list.sort((a, b) -> b.getValue().compareTo(a.getValue()));\n\n        for (Map.Entry<String, Integer> entry : list) {\n            System.out.println(entry.getKey() + \": \" + entry.getValue());\n        }\n\n        sc.close();\n    }\n}",
      "JavaScript": "const readline = require('readline');\n\nfunction main() {\n    const rl = readline.createInterface({\n        input: process.stdin,\n        output: process.stdout\n    });\n\n    let inputLines = [];\n    \n    rl.on('line', (line) => {\n        inputLines.push(line);\n        if (inputLines.length === parseInt(inputLines[0].split(' ')[0]) + 1) {\n            rl.close();\n        }\n    });\n\n    rl.on('close', () => {\n        // Extract the number of pairs and the divisor k\n        let [n, k] = inputLines[0].split(' ').map(Number);\n        let pairs = inputLines.slice(1);\n        \n        // Iterate through pairs to find and print those whose sum is divisible by k\n        pairs.forEach(pairLine => {\n            let [a, b] = pairLine.split(' ').map(Number);\n            if ((a + b) % k === 0) {\n                console.log(`(${a}, ${b})`);\n            }\n        });\n    });\n}\n\nmain();\n"
    }
  }
}