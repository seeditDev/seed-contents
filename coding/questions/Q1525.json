{
  "questionId": "Q1525",
  "title": "Maximum Path Sum in a Binary Tree",
  "slug": "maximum-path-sum-in-a-binary-tree",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-01",
  "metadata": {
    "category": "Trees",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "trees",
      "data structures and algorithms"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ]
  },
  "content": {
    "problemStatement": "A **path** in a binary tree is a sequence of nodes where each pair of consecutive nodes is directly connected by an edge. A node can appear in the path **only once**. The path does not necessarily have to go through the root.\n\nThe **path sum** is the sum of all node values in the path.\n\nGiven the `root` of a binary tree, return the **maximum path sum** among all possible **non-empty** paths.\n\nFunction Declaration\n\n**Function Name**\n\n\n\nmaxPathSummaxPathSummaxPathSum\n\n\n\n**Parameters**\n\n\n\nrootrootroot: The root node of the binary tree.\n\n\n\n**Return Value**\n\n\n\nReturns an integer representing the maximum path sum among all possible non-empty paths in the binary tree.",
    "constraints": [
      "The number of nodes in the tree is in the range [1,(3×104)][1, (3 \\times 10^{4})][1,(3×104)].",
      "−1000≤Node.val≤1000-1000 \\leq \\text{Node.val} \\leq 1000−1000≤Node.val≤1000",
      "Input Format",
      "The first line of input contains a single integer TTT, denoting the number of test cases.",
      "Each test case consists of **two lines of input**:",
      "The first line contains a single integer NNN — the number of nodes in the binary tree.",
      "The second line contains NNN space-separated integers representing the **level-order traversal** of the tree, where",
      "**NULL denotes a null child**.",
      "Output Format",
      "For each test case, output on a new line a single integer —",
      "the **maximum path sum** in the given binary tree."
    ],
    "inputFormat": "",
    "outputFormat": "",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n5\n5 4 8 11 13\n3\n2 NULL NULL",
        "output": "30\n2",
        "explanation": "Explanation:\nFirst test case:\nThe maximum path sum is 30, obtained by choosing the path 8 -> 5 -> 4 -> 13.\nThis path gives the highest possible sum among all valid paths in the tree.\n\n\nSecond test case:\nThe maximum path sum is 2, achieved by selecting the single node [2].\nIncluding NULLNULLNULL would decrease the total sum, so it is excluded."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "input": "2\n5\n5 4 8 11 13\n3\n2 NULL NULL",
        "expectedOutput": "30\n2",
        "weight": 100
      }
    ],
    "totalTestCases": 1,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      100
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "This is the most efficient and widely accepted solution.",
    "timeComplexity": "O(N)",
    "spaceComplexity": "O(H), where H is the height of the tree (recursion stack)",
    "code": {
      "C++": "class Solution {\npublic:\n    int ans;\n\n    int dfs(TreeNode* root) {\n        if (!root) return 0;\n\n        int left = max(0, dfs(root->left));\n        int right = max(0, dfs(root->right));\n\n        ans = max(ans, root->val + left + right);\n        return root->val + max(left, right);\n    }\n\n    int maxPathSum(TreeNode* root) {\n        ans = INT_MIN;\n        dfs(root);\n        return ans;\n    }\n};",
      "Python3": "class Solution:\n    def __init__(self):\n        self.ans = -10**18\n\n    def dfs(self, root):\n        if not root:\n            return 0\n\n        left = max(0, self.dfs(root.left))\n        right = max(0, self.dfs(root.right))\n\n        self.ans = max(self.ans, root.val + left + right)\n\n        return root.val + max(left, right)\n\n    def maxPathSum(self, root):\n        self.ans = -10**18\n        self.dfs(root)\n        return self.ans\n",
      "C": "int max(int a, int b) {\n    return a > b ? a : b;\n}\n\n/* DFS for max path sum */\nint dfs(TreeNode* root, int* ans) {\n    if (!root) return 0;\n\n    int left = max(0, dfs(root->left, ans));\n    int right = max(0, dfs(root->right, ans));\n\n    *ans = max(*ans, root->val + left + right);\n    return root->val + max(left, right);\n}\n\nint maxPathSum(TreeNode* root) {\n    int ans = INT_MIN;\n    dfs(root, &ans);\n    return ans;\n}",
      "Java": "class Solution {\n    private int ans;\n\n    private int dfs(TreeNode root) {\n        if (root == null) return 0;\n\n        int left = Math.max(0, dfs(root.left));\n        int right = Math.max(0, dfs(root.right));\n\n        ans = Math.max(ans, root.val + left + right);\n\n        return root.val + Math.max(left, right);\n    }\n\n    public int maxPathSum(TreeNode root) {\n        ans = Integer.MIN_VALUE;\n        dfs(root);\n        return ans;\n    }\n}",
      "JavaScript": "class Solution {\n    constructor() {\n        this.ans = Number.MIN_SAFE_INTEGER;\n    }\n\n    dfs(root) {\n        if (!root) return 0;\n\n        const left = Math.max(0, this.dfs(root.left));\n        const right = Math.max(0, this.dfs(root.right));\n\n        this.ans = Math.max(this.ans, root.val + left + right);\n\n        return root.val + Math.max(left, right);\n    }\n\n    maxPathSum(root) {\n        this.ans = Number.MIN_SAFE_INTEGER;\n        this.dfs(root);\n        return this.ans;\n    }\n}"
    }
  }
}