{
  "questionId": "Q1394",
  "title": "Pascal Triangle Element",
  "slug": "pascal-triangle-element",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-01",
  "metadata": {
    "category": "Recursion",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "recursion & backtracking",
      "data structures and algorithms"
    ],
    "companyTags": [],
    "topicTags": [
      "Recursion & Backtracking"
    ]
  },
  "content": {
    "problemStatement": "Given an integer NNN, representing the row number, and an integer MMM, representing the column number, find the value at the intersection of the NNNth row and MMMth column of Pascal's Triangle.\n\nPascal's Triangle is defined as follows:\n\nThe 000th row is 1.\nEach element in subsequent rows is the sum of the two elements directly above it in the previous row:\n\nFunction DeclarationFunction NamegetPascalElementgetPascalElementgetPascalElement – This function returns the element at the given row and column of Pascal’s Triangle.\n\nParameters\nNNN : An integer representing the row number in Pascal’s Triangle (0-based indexing).\nMMM : An integer representing the column number in Pascal’s Triangle (0-based indexing).\nReturn Value\nReturns an integer representing the value at row **N** and column **M** of Pascal’s Triangle.\nConstraints\n0≤N≤200 \\leq N \\leq 200≤N≤20\n0≤M≤N0 \\leq M \\leq N0≤M≤N\n\nInput Format\n\n\nThe input consists of a single line containing two space-separated integers:\n\n\n\n**`N`** – the row number\n**`M`** – the column number\n\n\n\n\nOutput Format\n\nPrint a single integer — the element at the **Mᵗʰ column** of the **Nᵗʰ row** in Pascal’s Triangle.",
    "constraints": [],
    "inputFormat": "",
    "outputFormat": "",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4 2",
        "output": "6",
        "explanation": "Explanation:\nThe 4th row of Pascal’s Triangle is [1, 4, 6, 4, 1].\nThe element at the 2nd column (0-based index) in the 4th row is 6."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "input": "4 2",
        "expectedOutput": "6",
        "weight": 100
      }
    ],
    "totalTestCases": 1,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      100
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The logic is based on the fact that each element in Pascal’s Triangle is the sum of the two elements directly above it.\n\n\n\n**Base Case**: If M=0 or M=N, the answer is `1`. This corresponds to the edges of Pascal’s Triangle, where the first and last elements of each row are always `1`. An additional check ensures that the column value M is within a valid range. If M&gt;N, it’s an invalid input.\n\n\n\n\n**Recursive Case**: For other positions, the value at the Nth row and Mth column is the sum of the values from the previous row:\n\nPascal’s value at (N, M) = Pascal’s value at (N−1, M−1) + Pascal’s value at (N−1, M).",
    "timeComplexity": "Since this recursive solution has overlapping subproblems, its time complexity is exponential, specifically O(2^N) because each recursive call contains two more recursive calls, leading to a binary tree-like recursion.",
    "spaceComplexity": "O(N) because of the recursion stack, where N is the depth of the recursion (equal to the row number).",
    "code": {
      "C++": "int getPascalElement(int N, int M) {\n    // Base cases\n    if (M == 0 || M == N) {\n        return 1;\n    }\n\n    // Recursive relation\n    return getPascalElement(N - 1, M - 1) + getPascalElement(N - 1, M);\n}",
      "Python3": "def getPascalElement(N, M):\n    # Base cases\n    if M == 0 or M == N:\n        return 1\n\n    # Recursive relation\n    return getPascalElement(N - 1, M - 1) + getPascalElement(N - 1, M)",
      "C": "int getPascalElement(int N, int M) {\n    // Base cases\n    if (M == 0 || M == N) {\n        return 1;\n    }\n\n    // Recursive relation\n    return getPascalElement(N - 1, M - 1) + getPascalElement(N - 1, M);\n}",
      "Java": "static int getPascalElement(int N, int M) {\n    // Base cases\n    if (M == 0 || M == N) {\n        return 1;\n    }\n\n    // Recursive relation\n    return getPascalElement(N - 1, M - 1) + getPascalElement(N - 1, M);\n}",
      "JavaScript": "function getPascalElement(N, M) {\n    // Base cases\n    if (M === 0 || M === N) {\n        return 1;\n    }\n\n    // Recursive relation\n    return getPascalElement(N - 1, M - 1) + getPascalElement(N - 1, M);\n}"
    }
  }
}