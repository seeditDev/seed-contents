{
  "questionId": "Q11870",
  "title": "Expression Add Operators",
  "slug": "expression-add-operators",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Backtracking",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "String",
      "Math",
      "Backtracking"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given a string num that contains only digits and an integer target, return all possibilities to insert the binary operators **'+'**, **'-'**, and/or **'*'** between the digits of num so that the resultant expression evaluates to the target value.\n\n\n\nNote that operands in the returned expressions should not contain leading zeros.\n\n\n\n**Note** that a number can contain multiple digits.",
    "constraints": [
      "1 <= num.length <= 10",
      "num contains only digits",
      "-2^31 <= target <= 2^31 - 1"
    ],
    "inputFormat": "num = string, target = integer",
    "outputFormat": "list of strings",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "num = \"123\", target = 6",
        "output": "[\"1*2*3\",\"1+2+3\"]",
        "explanation": "Both \"1*2*3\" and \"1+2+3\" evaluate to 6.\n\n**Example 2:**"
      },
      {
        "id": "sample_2",
        "input": "num = \"232\", target = 8",
        "output": "[\"2*3+2\",\"2+3*2\"]",
        "explanation": "Both \"2*3+2\" and \"2+3*2\" evaluate to 8.\n        \n    \n\n\n**Disclaimer**: Here is the practice link to help you assess your knowledge better. It's highly recommend trying to solve it before looking at the solution."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": "num = \"1\", target = 1",
        "expectedOutput": "[\"1\"]",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": "num = \"1234567890\", target = 45",
        "expectedOutput": "[]",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform case",
        "input": "num = \"111\", target = 3",
        "expectedOutput": "[\"1+1+1\"]",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "num = \"00\", target = 0",
        "expectedOutput": "[\"0+0\", \"0-0\"]",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": "num = \"123\", target = 6",
        "expectedOutput": "[\"1*2*3\",\"1+2+3\"]",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Depth-First Search (DFS)",
    "timeComplexity": "O(4^n)",
    "spaceComplexity": "O(n)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\nvoid dfs(char* num, int target, int start, long long current_value, long long last_operand, char* expression, char*** result, int* returnSize) {\n    if (start == strlen(num)) {\n        if (current_value == target) {\n            (*returnSize)++;\n            (*result) = realloc((*result), (*returnSize) * sizeof(char*));\n            (*result)[(*returnSize) - 1] = strdup(expression);\n        }\n        return;\n    }\n    for (int i = start; i < strlen(num); i++) {\n        if (i > start && num[start] == '0') return;\n        char current_num[10];\n        strncpy(current_num, num + start, i - start + 1);\n        current_num[i - start + 1] = '\\0';\n        long long current_num_val = atoll(current_num);\n        if (start == 0) {\n            char new_expression[100];\n            sprintf(new_expression, \"%s\", current_num);\n            dfs(num, target, i + 1, current_num_val, current_num_val, new_expression, result, returnSize);\n        } else {\n            char new_expression[100];\n            sprintf(new_expression, \"%s+%s\", expression, current_num);\n            dfs(num, target, i + 1, current_value + current_num_val, current_num_val, new_expression, result, returnSize);\n            sprintf(new_expression, \"%s-%s\", expression, current_num);\n            dfs(num, target, i + 1, current_value - current_num_val, -current_num_val, new_expression, result, returnSize);\n            sprintf(new_expression, \"%s*%s\", expression, current_num);\n            dfs(num, target, i + 1, current_value - last_operand + last_operand * current_num_val, last_operand * current_num_val, new_expression, result, returnSize);\n        }\n    }\n}\n\nchar** addOperators(char* num, int target, int* returnSize) {\n    char*** result = malloc(sizeof(char**));\n    *returnSize = 0;\n    dfs(num, target, 0, 0, 0, \"\", result, returnSize);\n    return *result;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<string> addOperators(string num, int target) {\n        vector<string> result;\n        dfs(num, target, 0, 0, 0, \"\", result);\n        return result;\n    }\n    void dfs(string& num, int target, int start, long long current_value, long long last_operand, string expression, vector<string>& result) {\n        if (start == num.size()) {\n            if (current_value == target) result.push_back(expression);\n            return;\n        }\n        for (int i = start; i < num.size(); i++) {\n            if (i > start && num[start] == '0') return;\n            string current_num = num.substr(start, i - start + 1);\n            long long current_num_val = stoll(current_num);\n            if (start == 0) {\n                dfs(num, target, i + 1, current_num_val, current_num_val, current_num, result);\n            } else {\n                dfs(num, target, i + 1, current_value + current_num_val, current_num_val, expression + \"+\" + current_num, result);\n                dfs(num, target, i + 1, current_value - current_num_val, -current_num_val, expression + \"-\" + current_num, result);\n                dfs(num, target, i + 1, current_value - last_operand + last_operand * current_num_val, last_operand * current_num_val, expression + \"*\" + current_num, result);\n            }\n        }\n    }\n};",
      "Java": "import java.util.ArrayList;\nimport java.util.List;\n\nclass Solution {\n    public List<String> addOperators(String num, int target) {\n        List<String> result = new ArrayList<>();\n        dfs(num, target, 0, 0, 0, \"\", result);\n        return result;\n    }\n    public void dfs(String num, int target, int start, long current_value, long last_operand, String expression, List<String> result) {\n        if (start == num.length()) {\n            if (current_value == target) result.add(expression);\n            return;\n        }\n        for (int i = start; i < num.length(); i++) {\n            if (i > start && num.charAt(start) == '0') return;\n            String current_num = num.substring(start, i + 1);\n            long current_num_val = Long.parseLong(current_num);\n            if (start == 0) {\n                dfs(num, target, i + 1, current_num_val, current_num_val, current_num, result);\n            } else {\n                dfs(num, target, i + 1, current_value + current_num_val, current_num_val, expression + \"+\" + current_num, result);\n                dfs(num, target, i + 1, current_value - current_num_val, -current_num_val, expression + \"-\" + current_num, result);\n                dfs(num, target, i + 1, current_value - last_operand + last_operand * current_num_val, last_operand * current_num_val, expression + \"*\" + current_num, result);\n            }\n        }\n    }\n}",
      "Python3": "class Solution:\n    def addOperators(self, num: str, target: int) -> List[str]:\n        result = []\n        self.dfs(num, target, 0, 0, 0, \"\", result)\n        return result\n    def dfs(self, num: str, target: int, start: int, current_value: int, last_operand: int, expression: str, result: List[str]) -> None:\n        if start == len(num):\n            if current_value == target: result.append(expression)\n            return\n        for i in range(start, len(num)):\n            if i > start and num[start] == '0': return\n            current_num = num[start:i+1]\n            current_num_val = int(current_num)\n            if start == 0:\n                self.dfs(num, target, i + 1, current_num_val, current_num_val, current_num, result)\n            else:\n                self.dfs(num, target, i + 1, current_value + current_num_val, current_num_val, expression + \"+\" + current_num, result)\n                self.dfs(num, target, i + 1, current_value - current_num_val, -current_num_val, expression + \"-\" + current_num, result)\n                self.dfs(num, target, i + 1, current_value - last_operand + last_operand * current_num_val, last_operand * current_num_val, expression + \"*\" + current_num, result)",
      "JavaScript": "var addOperators = function(num, target) {\n    let result = [];\n    dfs(num, target, 0, 0, 0, \"\", result);\n    return result;\n};\n\nvar dfs = function(num, target, start, current_value, last_operand, expression, result) {\n    if (start === num.length) {\n        if (current_value === target) result.push(expression);\n        return;\n    }\n    for (let i = start; i < num.length; i++) {\n        if (i > start && num[start] === '0') return;\n        let current_num = num.slice(start, i + 1);\n        let current_num_val = parseInt(current_num);\n        if (start === 0) {\n            dfs(num, target, i + 1, current_num_val, current_num_val, current_num, result);\n        } else {\n            dfs(num, target, i + 1, current_value + current_num_val, current_num_val, expression + \"+\" + current_num, result);\n            dfs(num, target, i + 1, current_value - current_num_val, -current_num_val, expression + \"-\" + current_num, result);\n            dfs(num, target, i + 1, current_value - last_operand + last_operand * current_num_val, last_operand * current_num_val, expression + \"*\" + current_num, result);\n        }\n    }\n};"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}