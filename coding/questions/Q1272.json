{
  "questionId": "Q1272",
  "title": "Finding least significant, most significant bit",
  "slug": "finding-least-significant-most-significant-bit",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-01",
  "metadata": {
    "category": "Bit Manipulation",
    "difficulty": "Easy",
    "isPremium": false,
    "tags": [
      "bit manipulation",
      "data structures and algorithms"
    ],
    "companyTags": [],
    "topicTags": [
      "Bit Manipulation"
    ]
  },
  "content": {
    "problemStatement": "Finding Least significant bitThe least significant bit (LSB) of a binary number is the rightmost bit. You can calculate it using the bitwise AND operation with 1.\n\n`   lsb = (num &amp; 1)\n`Finding Most significant bitTo find the most significant bit (MSB) of an integer, you can iterate through its binary representation and identify the leftmost non-zero bit.\nThe pseudocode to find the position of the most significant bit (MSB) in a positive integer:\n\n`function find_msb_position(number):\n    if number &lt;= 0:\n        raise ValueError(\"Input must be a positive integer\")\n\n    msb_position = 0\n\n    while number &gt; 0:\n        number = number &gt;&gt; 1  // Right shift by 1 to move to the next bit\n        msb_position = msb_position + 1\n\n    return msb_position - 1  // Subtract 1 to get the 0-based position\n`Task\nWrite a program to input an integer N.\nPrint the position of the most significant bit, and print the least significant bit.",
    "constraints": [],
    "inputFormat": "",
    "outputFormat": "",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "15",
        "output": "3 1",
        "explanation": ""
      },
      {
        "id": "sample_2",
        "input": "24",
        "output": "4 0",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "input": "15",
        "expectedOutput": "3 1",
        "weight": 50
      },
      {
        "id": "tc_2",
        "input": "24",
        "expectedOutput": "4 0",
        "weight": 50
      }
    ],
    "totalTestCases": 2,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      50,
      50
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The code idea is to determine two key properties of the input integer **N**: the position of the most significant bit (MSB) and the value of the least significant bit (LSB).\n\n\n\n**Least Significant Bit (LSB)**: This is obtained by performing a bitwise AND operation between **N** and `1`. If **N** is odd, the LSB will be `1`, and if it is even, the LSB will be `0`.\n\n\n\n\n**Most Significant Bit (MSB)**: The position of the MSB is found by repeatedly dividing **N** by `2` until **N** becomes `0`, while keeping track of the number of divisions. The position of the MSB is then the count of divisions minus one since the counting starts from `1`.",
    "timeComplexity": "O(N)",
    "spaceComplexity": "O(1)",
    "code": {}
  }
}