{
  "questionId": "Q3535",
  "title": "Bob vs ATM",
  "slug": "bob-vs-atm",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-02",
  "metadata": {
    "category": "Arrays",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "game theory",
      "competitive programming"
    ],
    "companyTags": [],
    "topicTags": [
      "Game Theory"
    ]
  },
  "content": {
    "problemStatement": "Due to a shortage of ATMs, a bank decided to convert some arcade gaming machines into ATMs. Unfortunately, due to a bug, the converted machine will only dispense cash if the withdrawer can win the game. The game is played over a valid bracket sequence. Formally, a valid bracket sequence is defined as:\n\n\nAn empty string is valid.\nIf `A` is a valid bracket sequence, then `(A)` is also valid.\nIf `A` and `B` are valid, `AB` is also valid.\nIn each move, a player can erase a non-empty valid substring of the remaining brackets such that it cannot be formed by concatenating two valid sequences. For example, a player can erase `(())` or `(()())` but not `()()` or `(())()`. The ATM always plays first. The player that is not able to make a move will lose the game. Assume that both players play optimally.\n\nBob is trying to withdraw money and is frustrated as he keeps losing against the machine. He suspects the machine is rigged and always wins the game. So, he has asked you to help him out. Given a valid bracket sequence, can you determine who wins the game?\n\n\nInput Format\nThe first line of the input contains an integer `T` denoting the number of test cases. The description of `T` test cases follows.\n\n\nEach test case consists of a single line containing the string `S` - the initial bracket sequence.\n\n\n\nOutput Format\nFor each test case, output a single line containing the winner - either \"ATM\" or \"Bob\" without quotes.",
    "constraints": [
      "`1 ≤ T ≤ 10^5`",
      "`2 ≤ length of S ≤ 10^5`",
      "The sum of all lengths of `S` will be not more than `10^6`",
      "`S` will be a valid bracket sequence"
    ],
    "inputFormat": "",
    "outputFormat": "",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5\n()\n(()()(()))\n()()\n(())()\n(()()(()))((()))",
        "output": "ATM\nATM\nBob\nATM\nBob",
        "explanation": "Explanation:\nExample case 1: The ATM will erase the entire sequence and win the game.\nExample case 2: The ATM will again erase the entire sequence and win the game.\nExample case 3: The ATM can erase either the first or the second (), Bob will erase the other one and win."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "input": "5\n()\n(()()(()))\n()()\n(())()\n(()()(()))((()))",
        "expectedOutput": "ATM\nATM\nBob\nATM\nBob",
        "weight": 100
      }
    ],
    "totalTestCases": 1,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      100
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "",
    "timeComplexity": "O(N)",
    "spaceComplexity": "O(1)",
    "code": {}
  }
}