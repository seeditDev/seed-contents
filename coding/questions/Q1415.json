{
  "questionId": "Q1415",
  "title": "Wildfire",
  "slug": "wildfire",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-01",
  "metadata": {
    "category": "Arrays",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "binary search",
      "data structures and algorithms"
    ],
    "companyTags": [],
    "topicTags": [
      "Binary Search"
    ]
  },
  "content": {
    "problemStatement": "A wildfire is approaching, and you need to set up emergency water sprinklers to protect all houses along a straight road. Each sprinkler has a fixed coverage radius, meaning it can cover houses within a certain distance from its position. Your task is to determine the minimum radius required for the sprinklers so that every house on the road is within the coverage of at least one sprinkler.\n\nGiven the positions of the houses and sprinklers on a horizontal line, calculate the smallest coverage radius that ensures all houses are protected. Note that all sprinklers must use the same coverage radius.\n\n\nInput Format\n\nThe first line contains an integer nnn, the number of houses.\nThe second line contains nnn space-separated integers, where each integer houses[i]houses[i]houses[i] represents the position of the iii-th house on the road.\nThe third line contains an integer mmm, the number of sprinklers.\nThe fourth line contains mmm space-separated integers, where each integer sprinklers[i]sprinklers[i]sprinklers[i] represents the position of the iii-th sprinkler on the road.\n\n\nOutput Format\n\nA single integer representing the minimum radius required for the sprinklers to ensure all houses are protected.",
    "constraints": [
      "1≤houses.length,sprinklers.length≤1051 \\leq \\text{houses.length}, \\text{sprinklers.length} \\leq 10^51≤houses.length,sprinklers.length≤105",
      "1≤houses[i],sprinklers[i]≤1091 \\leq \\text{houses}[i], \\text{sprinklers}[i] \\leq 10^91≤houses[i],sprinklers[i]≤109"
    ],
    "inputFormat": "",
    "outputFormat": "",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n1 2 3 4\n2\n1 4",
        "output": "1",
        "explanation": "Explanation:\nThe two sprinklers are placed at positions 1 and 4.\nThe first sprinkler at position 1 covers houses at positions 1 and 2.\nThe second sprinkler at position 4 covers houses at positions 3 and 4.\nA radius of 1 ensures that every house is protected."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "input": "4\n1 2 3 4\n2\n1 4",
        "expectedOutput": "1",
        "weight": 100
      }
    ],
    "totalTestCases": 1,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      100
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "",
    "timeComplexity": "O(N)",
    "spaceComplexity": "O(1)",
    "code": {
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nbool check(vector<int>& houses, vector<int>& sprinklers, int r){\n    int n = houses.size();\n    vector<int> extinguished(n, 0);\n    for(int s: sprinklers) {\n        int left = lower_bound(begin(houses), end(houses), s - r) - houses.begin();\n        int right = upper_bound(begin(houses), end(houses), s + r) - houses.begin();\n        if(left != n)\n            extinguished[left]++;\n        if(right < n)\n            extinguished[right]--;\n    }\n    for(int i = 0; i < n; i++){\n        if(i > 0)\n            extinguished[i] += extinguished[i - 1];\n        if(extinguished[i] <= 0)\n            return false;\n    }\n    return true;\n}\n\nint findRadius(vector<int>& houses, vector<int>& sprinklers) {\n    sort(begin(houses), end(houses));\n    sort(begin(sprinklers), end(sprinklers));\n    int low = 0, high = 1e9, mid, ans;\n    while(low <= high){\n        mid = (low + high) / 2;\n        if(check(houses, sprinklers, mid)){\n            ans = mid;\n            high = mid - 1;\n        } else {\n            low = mid + 1;\n        }\n    }\n    return ans;\n}\nint main() {\n    int n, m;\n    cin >> n;\n    vector<int> houses(n);\n    for(int &val: houses)\n      cin >> val;\n    cin >> m;\n    vector<int> sprinklers(m);\n    for(int &val: sprinklers)\n      cin >> val;\n    cout << findRadius(houses, sprinklers);\n    return 0;\n}",
      "Python3": "import sys\nimport bisect\n\ndef check(houses, sprinklers, r):\n    n = len(houses)\n    extinguished = [0] * (n + 1)  # Extra space to avoid index errors\n\n    for s in sprinklers:\n        left = bisect.bisect_left(houses, s - r)  # First position >= (s - r)\n        right = bisect.bisect_right(houses, s + r)  # First position > (s + r)\n\n        if left < n:\n            extinguished[left] += 1\n        if right < n:\n            extinguished[right] -= 1\n\n    coverage = 0\n    for i in range(n):\n        if i > 0:\n            extinguished[i] += extinguished[i - 1]\n        if extinguished[i] == 0:\n            return False  # Uncovered house exists\n\n    return True\n\ndef findRadius(houses, sprinklers):\n    houses.sort()\n    sprinklers.sort()\n\n    low, high, ans = 0, 10**9, -1\n    while low <= high:\n        mid = (low + high) // 2\n        if check(houses, sprinklers, mid):\n            ans = mid\n            high = mid - 1\n        else:\n            low = mid + 1\n    return ans\n\nif __name__ == \"__main__\":\n    # Read input\n    n = int(sys.stdin.readline().strip())\n    houses = list(map(int, sys.stdin.readline().strip().split()))\n    m = int(sys.stdin.readline().strip())\n    sprinklers = list(map(int, sys.stdin.readline().strip().split()))\n\n    # Output the result\n    print(findRadius(houses, sprinklers))\n",
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint compare(const void *a, const void *b) {\n    return (*(int*)a - *(int*)b);\n}\n\n// Custom lower_bound equivalent function\nint lower_bound(int *arr, int size, int value) {\n    int left = 0, right = size;\n    while (left < right) {\n        int mid = (left + right) / 2;\n        if (arr[mid] < value)\n            left = mid + 1;\n        else\n            right = mid;\n    }\n    return left;\n}\n\n// Custom upper_bound equivalent function\nint upper_bound(int *arr, int size, int value) {\n    int left = 0, right = size;\n    while (left < right) {\n        int mid = (left + right) / 2;\n        if (arr[mid] <= value)\n            left = mid + 1;\n        else\n            right = mid;\n    }\n    return left;\n}\n\nint check(int *houses, int n, int *sprinklers, int m, int r) {\n    int *extinguished = (int*)calloc(n, sizeof(int));\n    if (!extinguished) return 0;\n\n    for (int i = 0; i < m; i++) {\n        int s = sprinklers[i];\n        int left = lower_bound(houses, n, s - r);\n        int right = upper_bound(houses, n, s + r);\n\n        if (left < n) extinguished[left]++;\n        if (right < n) extinguished[right]--;\n    }\n\n    int covered = 0;\n    for (int i = 0; i < n; i++) {\n        if (i > 0) extinguished[i] += extinguished[i - 1];\n        if (extinguished[i] <= 0) {\n            free(extinguished);\n            return 0;\n        }\n    }\n    free(extinguished);\n    return 1;\n}\n\nint findRadius(int *houses, int n, int *sprinklers, int m) {\n    qsort(houses, n, sizeof(int), compare);\n    qsort(sprinklers, m, sizeof(int), compare);\n\n    int low = 0, high = 1000000000, mid, ans = 0;\n    while (low <= high) {\n        mid = (low + high) / 2;\n        if (check(houses, n, sprinklers, m, mid)) {\n            ans = mid;\n            high = mid - 1;\n        } else {\n            low = mid + 1;\n        }\n    }\n    return ans;\n}\n\nint main() {\n    int n, m;\n    scanf(\"%d\", &n);\n    int *houses = (int*)malloc(n * sizeof(int));\n    for (int i = 0; i < n; i++)\n        scanf(\"%d\", &houses[i]);\n    \n    scanf(\"%d\", &m);\n    int *sprinklers = (int*)malloc(m * sizeof(int));\n    for (int i = 0; i < m; i++)\n        scanf(\"%d\", &sprinklers[i]);\n    \n    printf(\"%d\\n\", findRadius(houses, n, sprinklers, m));\n\n    free(houses);\n    free(sprinklers);\n    return 0;\n}\n",
      "Java": "import java.util.*;\n\npublic class Main {\n    public static boolean check(int[] houses, int[] sprinklers, int r) {\n        int n = houses.length;\n        int[] extinguished = new int[n];\n\n        for (int s : sprinklers) {\n            int left = Arrays.binarySearch(houses, s - r);\n            int right = Arrays.binarySearch(houses, s + r);\n\n            if (left < 0) left = -left - 1;\n            if (right < 0) right = -right - 1;\n            else right++;\n\n            if (left < n) extinguished[left]++;\n            if (right < n) extinguished[right]--;\n        }\n\n        int covered = 0;\n        for (int i = 0; i < n; i++) {\n            if (i > 0) extinguished[i] += extinguished[i - 1];\n            if (extinguished[i] <= 0) return false;\n        }\n        return true;\n    }\n\n    public static int findRadius(int[] houses, int[] sprinklers) {\n        Arrays.sort(houses);\n        Arrays.sort(sprinklers);\n\n        int low = 0, high = 1000000000, mid, ans = 0;\n        while (low <= high) {\n            mid = (low + high) / 2;\n            if (check(houses, sprinklers, mid)) {\n                ans = mid;\n                high = mid - 1;\n            } else {\n                low = mid + 1;\n            }\n        }\n        return ans;\n    }\n\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] houses = new int[n];\n        for (int i = 0; i < n; i++)\n            houses[i] = sc.nextInt();\n        \n        int m = sc.nextInt();\n        int[] sprinklers = new int[m];\n        for (int i = 0; i < m; i++)\n            sprinklers[i] = sc.nextInt();\n\n        System.out.println(findRadius(houses, sprinklers));\n        sc.close();\n    }\n}\n",
      "JavaScript": "const readline = require(\"readline\");\n\nfunction lowerBound(arr, target) {\n    let left = 0, right = arr.length;\n    while (left < right) {\n        let mid = Math.floor((left + right) / 2);\n        if (arr[mid] < target) left = mid + 1;\n        else right = mid;\n    }\n    return left;\n}\n\nfunction upperBound(arr, target) {\n    let left = 0, right = arr.length;\n    while (left < right) {\n        let mid = Math.floor((left + right) / 2);\n        if (arr[mid] <= target) left = mid + 1;\n        else right = mid;\n    }\n    return left;\n}\n\nfunction check(houses, sprinklers, r) {\n    let n = houses.length;\n    let extinguished = new Array(n).fill(0);\n\n    for (let s of sprinklers) {\n        let left = lowerBound(houses, s - r);\n        let right = upperBound(houses, s + r);\n\n        if (left < n) extinguished[left]++;\n        if (right < n) extinguished[right]--;\n    }\n\n    let coverage = 0;\n    for (let i = 0; i < n; i++) {\n        if (i > 0) extinguished[i] += extinguished[i - 1];\n        if (extinguished[i] <= 0) return false;\n    }\n    return true;\n}\n\nfunction findRadius(houses, sprinklers) {\n    houses.sort((a, b) => a - b);\n    sprinklers.sort((a, b) => a - b);\n\n    let low = 0, high = 1e9, ans = 0;\n    while (low <= high) {\n        let mid = Math.floor((low + high) / 2);\n        if (check(houses, sprinklers, mid)) {\n            ans = mid;\n            high = mid - 1;\n        } else {\n            low = mid + 1;\n        }\n    }\n    return ans;\n}\n\n// Input handling\nconst rl = readline.createInterface({\n    input: process.stdin,\n    output: process.stdout\n});\n\nlet inputLines = [];\nrl.on(\"line\", (line) => {\n    inputLines.push(line.trim());\n}).on(\"close\", () => {\n    let n = parseInt(inputLines[0]);\n    let houses = inputLines[1].split(\" \").map(Number);\n    let m = parseInt(inputLines[2]);\n    let sprinklers = inputLines[3].split(\" \").map(Number);\n\n    console.log(findRadius(houses, sprinklers));\n});\n"
    }
  }
}