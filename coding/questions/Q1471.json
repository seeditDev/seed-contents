{
  "questionId": "Q1471",
  "title": "Backtracking - Find Valid Parenthesis",
  "slug": "backtracking-find-valid-parenthesis",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Recursion & Backtracking",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Parentheses",
      "Recursion & Backtracking",
      "String Generation",
      "Backtracking"
    ],
    "companyTags": [],
    "topicTags": [
      "Recursion & Backtracking"
    ]
  },
  "content": {
    "problemStatement": "Given NNN pairs of parentheses. Find all **valid parentheses** string of length 2\u22c5N2 \\cdot N2\u22c5N.\n\nNote: A **valid parentheses** string is defined as:\n\n\nEmpty string is valid.\nIf PPP is valid, (P)(P)(P) is also valid.\nIf PPP and QQQ are valid, PQPQPQ is also valid.\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nEach test case consists of a single line of input, containing integer NNN.\n\n\nOutput Format\nFor each test case, output M+1M+1M+1 lines, where MMM is the number of valid parentheses:\n\n\n\nThe first line contains a single integer MMM.\nThe next MMM lines contain string of length 2\u22c5N2 \\cdot N2\u22c5N.\n\nNote: The valid parentheses strings must be printed in **lexicographically increasing** order.\nString SSS is said to be lexicographically smaller than string TTT if there exists a position iii where Si&lt;TiS_i \\lt T_iSi\u200b&lt;Ti\u200b and Sj=TjS_j = T_jSj\u200b=Tj\u200b for all j&lt;ij \\lt ij&lt;i.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 11"
    ],
    "inputFormat": "First line contains T, the number of test cases. Each test case contains a single integer N.",
    "outputFormat": "For each test case, print the number of valid parentheses strings of length 2*N on the first line, followed by each string on a new line.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n2\n3",
        "output": "2\n(())\n()()\n5\n((()))\n(()())\n(())()\n()(())\n()()()",
        "explanation": "Explanation:\nTest case 1: There will be 2 valid parenthesis strings (()) and ()().\nTest case 2: There will be 5 valid parenthesis strings ((())), (()()), (())(), ()(()) and ()()()."
      },
      {
        "id": "sample_2",
        "input": "1\n1",
        "output": "1\n()",
        "explanation": "Explanation:\nTest case 1: There will be 1 valid parenthesis string ()"
      },
      {
        "id": "sample_3",
        "input": "1\n4",
        "output": "14\n(((())))\n((()()))\n((())())\n((()))()\n(()(()))\n(()()())\n(()())()\n(())(()\n(())()()\n()((()))\n()(()())\n()(())()\n()()(())\n()()()()",
        "explanation": "Explanation:\nTest case 1: There will be 14 valid parenthesis strings"
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1",
        "expectedOutput": "1\n()",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n11",
        "expectedOutput": "The expected output is too large to be included here",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n5",
        "expectedOutput": "The expected output is too large to be included here",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n10",
        "expectedOutput": "The expected output is too large to be included here",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "5\n1\n2\n3\n4\n5",
        "expectedOutput": "The expected output is too large to be included here",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved using a backtracking approach. We start with an empty string and add either an open or close parenthesis at each step. We ensure that the number of close parentheses never exceeds the number of open parentheses. Once we have added 2*N parentheses, we add the string to our result set.",
    "timeComplexity": "O(4^N / N^(3/2)) per test case due to the Catalan number sequence, O(T * 4^N / N^(3/2)) total.",
    "spaceComplexity": "O(4^N / N^(3/2)) for storing the result set.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\nvoid generateParenthesis(int n, char** results, int* count) {\n    char* str = (char*)malloc((2 * n + 1) * sizeof(char));\n    int index = 0;\n\n    void _generateParenthesis(int pos, int open, int close) {\n        if (close == n) {\n            str[pos] = '\\0';\n            results[*count] = strdup(str);\n            (*count)++;\n            return;\n        } else {\n            if (open > close) {\n                str[pos] = ')';\n                _generateParenthesis(pos + 1, open, close + 1);\n            }\n            if (open < n) {\n                str[pos] = '(';\n                _generateParenthesis(pos + 1, open + 1, close);\n            }\n        }\n    }\n\n    _generateParenthesis(0, 0, 0);\n    free(str);\n}\n\nint compare(const void* a, const void* b) {\n    return strcmp(*(const char**)a, *(const char**)b);\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n\n    while (T--) {\n        int N;\n        scanf(\"%d\", &N);\n\n        int max_results = 1 << (2 * N);  // Upper bound on number of results\n        char** results = (char**)malloc(max_results * sizeof(char*));\n        int count = 0;\n\n        generateParenthesis(N, results, &count);\n        qsort(results, count, sizeof(char*), compare);\n\n        printf(\"%d\\n\", count);\n        for (int i = 0; i < count; i++) {\n            printf(\"%s\\n\", results[i]);\n            free(results[i]);\n        }\n        free(results);\n    }\n\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <string>\n#include <set>\n#include <map>\n#include <stack>\n#include <queue>\n#include <vector>\n#include <utility>\n#include <iomanip>\n#include <sstream>\n#include <bitset>\n#include <cstdlib>\n#include <iterator>\n#include <algorithm>\n#include <cstdio>\n#include <cctype>\n#include <cmath>\n#include <math.h>\n#include <ctime>\n#include <cstring>\n#include <unordered_set>\n#include <unordered_map>\n#include <cassert>\n#define int long long int\n#define pb push_back\n#define mp make_pair\n#define mod 1000000007\n#define vl vector <ll>\n#define all(c) (c).begin(),(c).end()\nusing namespace std;\n\nconst int N=500023;\nbool vis[N];\nvector <int> adj[N];\nlong long readInt(long long l,long long r,char endd){\n    long long x=0;\n    int cnt=0;\n    int fi=-1;\n    bool is_neg=false;\n    while(true){\n        char g=getchar();\n        if(g=='-'){\n            assert(fi==-1);\n            is_neg=true;\n            continue;\n        }\n        if('0'<=g && g<='9'){\n            x*=10;\n            x+=g-'0';\n            if(cnt==0){\n                fi=g-'0';\n            }\n            cnt++;\n            assert(fi!=0 || cnt==1);\n            assert(fi!=0 || is_neg==false);\n\n            assert(!(cnt>19 || ( cnt==19 && fi>1) ));\n        } else if(g==endd){\n            if(is_neg){\n                x= -x;\n            }\n\n            if(!(l <= x && x <= r))\n            {\n                cerr << l << ' ' << r << ' ' << x << '\\n';\n                assert(1 == 0);\n            }\n\n            return x;\n        } else {\n            assert(false);\n        }\n    }\n}\nstring readString(int l,int r,char endd){\n    string ret=\"\";\n    int cnt=0;\n    while(true){\n        char g=getchar();\n        assert(g!=-1);\n        if(g==endd){\n            break;\n        }\n        cnt++;\n        ret+=g;\n    }\n    assert(l<=cnt && cnt<=r);\n    return ret;\n}\nlong long readIntSp(long long l,long long r){\n    return readInt(l,r,' ');\n}\nlong long readIntLn(long long l,long long r){\n    return readInt(l,r,'\\n');\n}\nstring readStringLn(int l,int r){\n    return readString(l,r,'\\n');\n}\nstring readStringSp(int l,int r){\n    return readString(l,r,' ');\n}\n\nvoid solve(int idx,int cnta,int cntb,string &curr, vector<string>&ans,int n)\n{\n    if(idx>n)\n    {\n        if(cnta==cntb)\n        ans.pb(curr);\n        return;\n    }\n\n    if(cntb>cnta)\n    return;\n\n    // '('\n    curr.push_back('(');\n    solve(idx+1,cnta+1,cntb,curr,ans,n);\n    curr.pop_back();\n\n    // ')'\n    curr.push_back(')');\n    solve(idx+1,cnta,cntb+1,curr,ans,n);\n    curr.pop_back();\n}\n\nvoid solve()\n{\n    int n=readInt(1,11,'\\n');\n\n    vector<string>ans;\n    string curr;\n\n    solve(1,0,0,curr,ans,2*n);\n\n    cout<<ans.size()<<endl;\n    for(auto it: ans)\n    cout<<it<<endl;\n}\n\nint32_t main()\n{\n    #ifndef ONLINE_JUDGE\n    freopen(\"in.txt\", \"r\", stdin);\n    freopen(\"out.txt\", \"w\", stdout);\n    #endif\n    ios_base::sync_with_stdio(false);\n    cin.tie(NULL),cout.tie(NULL);\n    int T=readInt(1,10,'\\n');\n    \n    while(T--){\n        solve();\n    }\n    assert(getchar()==-1);\n    cerr << \"Time : \" << 1000 * ((double)clock()) / (double)CLOCKS_PER_SEC << \"ms\\n\";\n}",
      "Java": "public class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int T = sc.nextInt();\n        while (T-- > 0) {\n            int N = sc.nextInt();\n            Set<String> result = new HashSet<>();\n            solve(N, 0, 0, \"\", result);\n            List<String> list = new ArrayList<>(result);\n            Collections.sort(list);\n            System.out.println(list.size());\n            for (String s : list) {\n                System.out.println(s);\n            }\n        }\n    }\n\n    public static void solve(int n, int open, int close, String curr, Set<String> result) {\n        if (curr.length() == 2 * n) {\n            if (open == close) {\n                result.add(curr);\n            }\n            return;\n        }\n        if (open < n) {\n            solve(n, open + 1, close, curr + \"(\", result);\n        }\n        if (close < open) {\n            solve(n, open, close + 1, curr + \")\", result);\n        }\n    }\n}",
      "Python3": "def Solve(n,open1,close,res,result):\n    if len(res)==2*n:\n        if open1==close:\n            result.add(res)\n        return\n    if open1<n:\n        Solve(n,open1+1,close,res+'(',result)\n    if close<open1:\n        Solve(n,open1,close+1,res+')',result)\n\nfor _ in range(int(input())):\n    n=int(input())\n    result=set()\n    Solve(n,0,0,\"\",result)\n    print(len(result))\n    for i in sorted(result):\n        print(i)",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nfunction readLine() {\n    return lines[idx++];\n}\n\nfunction readInt() {\n    return parseInt(readLine());\n}\n\nfunction solve(n, open, close, curr, result) {\n    if (curr.length == 2 * n) {\n        if (open == close) {\n            result.push(curr);\n        }\n        return;\n    }\n    if (open < n) {\n        solve(n, open + 1, close, curr + '(', result);\n    }\n    if (close < open) {\n        solve(n, open, close + 1, curr + ')', result);\n    }\n}\n\nlet T = readInt();\nwhile (T-- > 0) {\n    let N = readInt();\n    let result = [];\n    solve(N, 0, 0, '', result);\n    result.sort();\n    console.log(result.length);\n    for (let i = 0; i < result.length; i++) {\n        console.log(result[i]);\n    }\n}"
    }
  }
}