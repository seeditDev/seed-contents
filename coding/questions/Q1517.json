{
  "questionId": "Q1517",
  "title": "Find how many times the array have been rotated",
  "slug": "find-how-many-times-the-array-have-been-rotated",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-01",
  "metadata": {
    "category": "Arrays",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "binary search",
      "data structures and algorithms"
    ],
    "companyTags": [],
    "topicTags": [
      "Binary Search"
    ]
  },
  "content": {
    "problemStatement": "You are given an array of integers numsnumsnums of size nnn, which was originally sorted in ascending order with **unique elements**. The array has been **rotated to the right** by an unknown number of steps (ranging from 0 to n - 1).\n\nYour task is to **find how many times the array has been rotated**.\n\nFunction DeclarationFunction NamecountRotationscountRotationscountRotations – This function returns the number of times a sorted array has been rotated.\n\nParameters\nnumsnumsnums : A reference to a rotated sorted array of unique integers.\nReturn Value\nReturns an integer representing the number of right rotations applied to the array.\nConstraints\n1≤n≤1041 \\leq n \\leq 10^41≤n≤104\n−104≤nums[i]≤104-10^4 \\leq nums[i] \\leq 10^4−104≤nums[i]≤104\nAll elements in numsnumsnums are **unique**\nnumsnumsnums is a rotated version of a sorted array\n\nInput Format\n\nThe first line contains an integer TTT — number of test cases.\nFor each test case:\n\nOne line containing an integer nnn — size of the array\nOne line containing nnn space-separated integers — the rotated sorted array\n\n\n\n\nOutput Format\n\nFor each test case, print a single integer — number of rotations",
    "constraints": [],
    "inputFormat": "",
    "outputFormat": "",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n7\n7 9 12 15 2 4 5\n6\n10 20 30 5 7 8\n5\n1 2 3 4 5",
        "output": "4\n3\n0",
        "explanation": "Explanation:\nFor the first test case the original sorted array is [2, 4, 5, 7, 9, 12, 15]. \nIt has been rotated 4 times to the right."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "input": "3\n7\n7 9 12 15 2 4 5\n6\n10 20 30 5 7 8\n5\n1 2 3 4 5",
        "expectedOutput": "4\n3\n0",
        "weight": 100
      }
    ],
    "totalTestCases": 1,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      100
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "",
    "timeComplexity": "O(N)",
    "spaceComplexity": "O(1)",
    "code": {
      "C++": "class Solution {\npublic:\n    int countRotations(const vector<int>& nums) {\n        int left = 0;\n        int right = nums.size() - 1;\n        int n = nums.size();\n\n        while (left <= right) {\n            // If the subarray is already sorted\n            if (nums[left] <= nums[right]) {\n                return left;\n            }\n\n            int mid = left + (right - left) / 2;\n            int next = (mid + 1) % n;\n            int prev = (mid + n - 1) % n;\n\n            // Check if mid is the minimum element\n            if (nums[mid] <= nums[next] && nums[mid] <= nums[prev]) {\n                return mid;\n            }\n\n            // Decide which half to search next\n            if (nums[mid] >= nums[left]) {\n                left = mid + 1;\n            } else {\n                right = mid - 1;\n            }\n        }\n\n        return 0;  // Array not rotated\n    }\n};\n",
      "Python3": "class Solution:\n    def count_rotations(self, nums):\n        left = 0\n        right = len(nums) - 1\n        n = len(nums)\n\n        while left <= right:\n            # If the subarray is already sorted\n            if nums[left] <= nums[right]:\n                return left\n\n            mid = left + (right - left) // 2\n            next_idx = (mid + 1) % n\n            prev_idx = (mid - 1 + n) % n\n\n            # Check if mid is the smallest element\n            if nums[mid] <= nums[next_idx] and nums[mid] <= nums[prev_idx]:\n                return mid\n\n            # Decide which half to search next\n            if nums[mid] >= nums[left]:\n                left = mid + 1\n            else:\n                right = mid - 1\n\n        return 0  # If array is not rotated\n",
      "C": "int countRotations(int nums[], int n) {\n    int left = 0;\n    int right = n - 1;\n\n    while (left <= right) {\n        // If the subarray is already sorted\n        if (nums[left] <= nums[right]) {\n            return left;\n        }\n\n        int mid = left + (right - left) / 2;\n        int next = (mid + 1) % n;\n        int prev = (mid + n - 1) % n;\n\n        // Check if mid is the minimum element\n        if (nums[mid] <= nums[next] && nums[mid] <= nums[prev]) {\n            return mid;\n        }\n\n        // Decide which half to search next\n        if (nums[mid] >= nums[left]) {\n            left = mid + 1;\n        } else {\n            right = mid - 1;\n        }\n    }\n\n    return 0;  // Array not rotated\n}\n",
      "Java": "class Solution {\n    public int countRotations(int[] nums) {\n        int left = 0;\n        int right = nums.length - 1;\n        int n = nums.length;\n\n        while (left <= right) {\n            // If the subarray is already sorted\n            if (nums[left] <= nums[right]) {\n                return left;\n            }\n\n            int mid = left + (right - left) / 2;\n            int next = (mid + 1) % n;\n            int prev = (mid - 1 + n) % n;\n\n            // Check if mid is the smallest element\n            if (nums[mid] <= nums[next] && nums[mid] <= nums[prev]) {\n                return mid;\n            }\n\n            // Decide which half to search next\n            if (nums[mid] >= nums[left]) {\n                left = mid + 1;\n            } else {\n                right = mid - 1;\n            }\n        }\n\n        return 0;  // If array is not rotated\n    }\n}\n",
      "JavaScript": "class Solution {\n    countRotations(nums) {\n        let left = 0;\n        let right = nums.length - 1;\n        const n = nums.length;\n\n        while (left <= right) {\n            // If subarray is already sorted\n            if (nums[left] <= nums[right]) {\n                return left;\n            }\n\n            const mid = Math.floor((left + right) / 2);\n            const next = (mid + 1) % n;\n            const prev = (mid - 1 + n) % n;\n\n            // Check if mid is the smallest element\n            if (nums[mid] <= nums[next] && nums[mid] <= nums[prev]) {\n                return mid;\n            }\n\n            // Decide which half to search next\n            if (nums[mid] >= nums[left]) {\n                left = mid + 1;\n            } else {\n                right = mid - 1;\n            }\n        }\n\n        return 0;  // If array is not rotated\n    }\n}\n"
    }
  }
}