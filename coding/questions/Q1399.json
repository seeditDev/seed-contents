{
  "questionId": "Q1399",
  "title": "Maximum Sum of K Elements",
  "slug": "maximum-sum-of-k-elements",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-01",
  "metadata": {
    "category": "Arrays",
    "difficulty": "Easy",
    "isPremium": false,
    "tags": [
      "two pointers & sliding window",
      "data structures and algorithms"
    ],
    "companyTags": [],
    "topicTags": [
      "Two Pointers & Sliding Window"
    ]
  },
  "content": {
    "problemStatement": "You are given an array AAA containing NNN elements and an integer KKK. You have to find the subarray with the maximum sum among all the K-sized sub-arrays and output this maximum sum.\n\nThis is a perfect example of a question that can be efficiently solved using the sliding window approach.\n\nNormally, we would calculate the sum of all K-sized sub-arrays which would have the time complexity of O(N)O(N)O(N).\n\nBut, using the Sliding Window approach we can assume the K-sized subarray to be a window. Now we just need to calculate the sum of the first window as the sum of the next window can be derived from the previous sum by subtracting the first element and adding the next element.\n\nFor example, let's take an array A=[1,2,3,4,5,6,7]A = [1, 2, 3, 4, 5, 6, 7]A=[1,2,3,4,5,6,7] and K=3K = 3K=3.\n\n\nLet's calculate the sum of the first window of three elements. 1+2+3=61+2+3 = 61+2+3=6\nNow the next window will have element 2,3,4{2, 3, 4}2,3,4.\nCompared to the previous window the first element (1)(1)(1) is removed and the next element (4)(4)(4) is added.\nSo, we can calculate the next window's sum by subtracting 111 and adding 444 to the sum of the previous window.\nThus, the sum of the next window will be 6−1+4=96-1+4 = 96−1+4=9\nSolve the above question using the Sliding Window approach\n\n\nInput Format\n\nThe first line of the input contains a single integer NNN and KKK, denoting the length of the array AAA and the length of the sub-array.\nThe second line of the input contains NNN space-separated integers A1,A2,…,ANA_1, A_2, \\ldots, A_NA1​,A2​,…,AN​ — denoting the array AAA.\n\n\nOutput Format\n\nOutput the maximum sum.",
    "constraints": [
      "2≤N≤1052 \\leq N \\leq 10^52≤N≤105",
      "1≤Ai≤1091 \\leq A_i \\leq 10^91≤Ai​≤109",
      "2≤K≤N2 \\leq K \\leq N2≤K≤N"
    ],
    "inputFormat": "",
    "outputFormat": "",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "10 3\n9 8 2 4 1 9 9 5 1 8",
        "output": "23",
        "explanation": "Explanation:\nThe subarray {9, 9, 5} has the maximum sum."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "input": "10 3\n9 8 2 4 1 9 9 5 1 8",
        "expectedOutput": "23",
        "weight": 100
      }
    ],
    "totalTestCases": 1,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      100
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The code idea involves using a **sliding window** to efficiently calculate the maximum sum of **K-sized** subarrays. After reading N and K, we initialize the sum with the first K elements and set it as the maximum sum. As we slide the window by updating the sum (subtracting the leftmost element and adding the next element), we continually check and update the maximum sum. This approach ensures we only traverse the array once, making it optimal for this problem.",
    "timeComplexity": "O(N)",
    "spaceComplexity": "O(1)",
    "code": {
      "C++": "#include <bits/stdc++.h>\n\nusing namespace std;\n\nint main() {\n    int n, k;\n    cin >> n >> k;\n    int arr[n];\n    for (int i = 0; i < n; i++) {\n        cin >> arr[i];\n    }\n    int i = 0;\n    long long sum = 0;\n    for (int i = 0; i < k; i++) {\n        sum += arr[i];\n    }\n    long long maxsum = sum;\n    int left = 0, right = k;\n    while (right < n) {\n        sum += arr[right] - arr[left];\n        right++;\n        left++;\n        maxsum = max(maxsum, sum);\n    }\n    cout << maxsum << endl;\n}",
      "Python3": "def max_subarray_sum(n, k, arr):\n    # Initialize sum with the sum of the first 'k' elements\n    current_sum = sum(arr[:k])\n    max_sum = current_sum\n    left = 0\n    right = k\n\n    # Sliding window to find the maximum sum of any subarray of length 'k'\n    while right < n:\n        current_sum += arr[right] - arr[left]\n        max_sum = max(max_sum, current_sum)\n        left += 1\n        right += 1\n\n    return max_sum\n\n# Read input values\nn, k = map(int, input().split())\narr = list(map(int, input().split()))\n\n# Call the function and print the result\nprint(max_subarray_sum(n, k, arr))\n",
      "C": "#include <stdio.h>\n\nint main() {\n    int n, k;\n    scanf(\"%d %d\", &n, &k);\n    int arr[n];\n    \n    for (int i = 0; i < n; i++) {\n        scanf(\"%d\", &arr[i]);\n    }\n\n    long long sum = 0;\n    for (int i = 0; i < k; i++) {\n        sum += arr[i];\n    }\n\n    long long maxsum = sum;\n    int left = 0, right = k;\n\n    while (right < n) {\n        sum += arr[right] - arr[left];\n        right++;\n        left++;\n        if (sum > maxsum) {\n            maxsum = sum;\n        }\n    }\n\n    printf(\"%lld\\n\", maxsum);\n    return 0;\n}\n",
      "Java": "import java.util.*;\nimport java.lang.*;\nimport java.io.*;\n\nclass Codechef{\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n\n        // Reading inputs\n        int n = scanner.nextInt();\n        int k = scanner.nextInt();\n        long[] arr = new long[n];\n\n        for (int i = 0; i < n; i++) {\n            arr[i] = scanner.nextLong();\n        }\n\n        long sum = 0;\n\n        // Calculate the initial sum of the first 'k' elements\n        for (int i = 0; i < k; i++) {\n            sum += arr[i];\n        }\n\n        long maxSum = sum;\n        int left = 0, right = k;\n\n        // Sliding window to find the maximum sum of 'k' consecutive elements\n        while (right < n) {\n            sum += arr[right] - arr[left];\n            right++;\n            left++;\n            maxSum = Math.max(maxSum, sum);\n        }\n\n        // Output the maximum sum\n        System.out.println(maxSum);\n\n        // Closing the scanner\n        scanner.close();\n    }\n}",
      "JavaScript": "function maxSubarraySum(n, k, arr) {\n    // Initialize sum with the sum of the first 'k' elements\n    let currentSum = 0;\n    for (let i = 0; i < k; i++) {\n        currentSum += arr[i];\n    }\n\n    let maxSum = currentSum;\n    let left = 0;\n    let right = k;\n\n    // Sliding window to find the maximum sum of any subarray of length 'k'\n    while (right < n) {\n        currentSum += arr[right] - arr[left];\n        maxSum = Math.max(maxSum, currentSum);\n        left += 1;\n        right += 1;\n    }\n\n    return maxSum;\n}\n\n// Read input values\nconst readline = require('readline');\n\nconst rl = readline.createInterface({\n    input: process.stdin,\n    output: process.stdout\n});\n\nrl.on('line', (input) => {\n    const inputs = input.split(' ').map(Number);\n    const n = inputs[0];\n    const k = inputs[1];\n\n    rl.on('line', (input) => {\n        const arr = input.split(' ').map(Number);\n        const result = maxSubarraySum(n, k, arr);\n        console.log(result);\n        rl.close();\n    });\n});\n"
    }
  }
}