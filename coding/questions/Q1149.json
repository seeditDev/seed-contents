{
  "questionId": "Q1149",
  "title": "Password generator - The Beginning",
  "slug": "password-generator-the-beginning",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-01",
  "metadata": {
    "category": "Strings",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "strings & streams",
      "programming languages"
    ],
    "companyTags": [],
    "topicTags": [
      "Strings & Streams"
    ]
  },
  "content": {
    "problemStatement": "Let us think a bit more about the Password construction scenario.\n\n\nPassword construction\n\nCreate a random password of length (N−2)(N - 2)(N−2) consisting only alphabets. Remember that the 1st character of the password should necessarily be an alphabet\nIf 'use_numbers' is 'Yes', then concatenate any random number at the end. If it is 'No', then concatenate any random alphabet at the end. Now the password string has length (N−1)(N - 1)(N−1).\nIf 'use_special_chars' is 'Yes', then concatenate any random special character at the end. If it is 'No', then concatenate any random alphabet at the end. Now the password string has length NNN.\n\n\nCheck the code given in the IDE to generate a random string of length 4 consisting of only alphabets.\n\nI've added comments explaining each part of the code to make it clearer - we are generating a password of length 4:\n\n\nThe `#include` directives bring in the necessary libraries: `&lt;iostream&gt;` for input and output operations, `&lt;cstdlib&gt;` for random number generation, and `&lt;ctime&gt;` for seeding the random number generator with the current time.\nThe `string_char` variable is defined as a `std::string` containing the characters from which the password will be generated.\nThe password variable is initialized as an empty `std::string` to store the generated password.\nThe `std::srand()` function seeds the random number generator with the current time by using the `std::time(0)` function. This ensures that the generated sequence of random numbers changes with each program execution.\nA for loop iterates exactly 4 times. During each iteration:\n\nA random index is generated using the modulo operator `(%)` with `string_char.size()` to ensure it is within the valid range of the string.\nThe character at the randomly chosen index in `string_char` is appended to the password string.\n\n\nFinally, the generated password is output using `std::cout`.",
    "constraints": [],
    "inputFormat": "",
    "outputFormat": "",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "",
        "output": "#include",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "input": "",
        "expectedOutput": "#include",
        "weight": 100
      }
    ],
    "totalTestCases": 1,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      100
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "",
    "timeComplexity": "",
    "spaceComplexity": "",
    "code": {}
  }
}