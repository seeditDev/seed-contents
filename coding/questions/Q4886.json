{
  "questionId": "Q4886",
  "title": "Scalar Product Tree",
  "slug": "scalar-product-tree",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Prefix Sums",
      "Depth First Search",
      "Graphs",
      "Binary Lifting",
      "Modulo Arithmetic",
      "Tree"
    ],
    "companyTags": [],
    "topicTags": [
      "Data Structures"
    ]
  },
  "content": {
    "problemStatement": "Chef has a tree with NNN vertices (numbered 111 through NNN). The tree is rooted at the vertex 111. For each valid iii, the weight of the iii-th vertex is wiw_iwi\u200b.\n\nFor a vertex uuu with depth ddd (the root has depth 111), let's denote the sequence of vertices on the path from uuu to the root by (v1,v2,\u2026,vd)(v_1, v_2, \\ldots, v_d)(v1\u200b,v2\u200b,\u2026,vd\u200b), where v1=uv_1 = uv1\u200b=u and vd=1v_d = 1vd\u200b=1. Then, let FuF_uFu\u200b be the vector (i.e. sequence) (wv1,wv2,\u2026,wvd)(w_{v_1}, w_{v_2}, \\ldots, w_{v_d})(wv1\u200b\u200b,wv2\u200b\u200b,\u2026,wvd\u200b\u200b), where the iii-th element is Fu,i=wviF_{u, i} = w_{v_i}Fu,i\u200b=wvi\u200b\u200b for each valid iii.\n\nFor any two vertices uuu and vvv with the same depth ddd, let's define the dot product Fu\u22c5Fv=\u2211i=1dFu,i\u22c5Fv,iF_u \\cdot F_v = \\sum_{i=1}^d F_{u,i} \\cdot F_{v,i}Fu\u200b\u22c5Fv\u200b=\u2211i=1d\u200bFu,i\u200b\u22c5Fv,i\u200b.\n\nYour task is to answer QQQ queries. In each query, you are given two vertices uuu and vvv with the same depth, and you should find Fu\u22c5FvF_u \\cdot F_vFu\u200b\u22c5Fv\u200b. Since the dot product can be large, compute it modulo 2322^{32}232.",
    "constraints": [
      "1 \u2264 T \u2264 5",
      "1 \u2264 N, Q \u2264 3\u00b710^5 (sum of N and sum of Q over all test cases \u2264 3\u00b710^5)",
      "1 \u2264 wi \u2264 10^9",
      "1 \u2264 u, v \u2264 N",
      "In each query, depth(u) = depth(v)"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case:\n- The first line contains two integers N and Q \u2014 the number of vertices in the tree and the number of queries.\n- The second line contains N integers w1,w2,\u2026,wN \u2014 the weight of each vertex (1\u2011indexed).\n- Each of the next N\u20111 lines contains two integers u and v denoting an undirected edge.\n- Each of the next Q lines contains two integers u and v describing a query. It is guaranteed that u and v have the same depth in the rooted tree (root is vertex 1).",
    "outputFormat": "For each query, output a single line containing Fu\u00b7Fv modulo 2^32.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5 2\n5 4 3 2 1\n1 2\n1 3\n2 4\n2 5\n2 3\n4 5",
        "output": "37\n43",
        "explanation": "Explanation:\nIn the first query, F2=(4,5)F_2 = (4,5)F2\u200b=(4,5) and F3=(3,5)F_3 = (3,5)F3\u200b=(3,5), so the scalar product is 4\u22c53+5\u22c55=374 \\cdot 3 + 5 \\cdot 5 = 374\u22c53+5\u22c55=37.\nIn the second query, F4=(2,4,5)F_4 = (2,4,5)F4\u200b=(2,4,5) and F5=(1,4,5)F_5 = (1,4,5)F5\u200b=(1,4,5), so the scalar product is 2\u22c51+4\u22c54+5\u22c55=432 \\cdot 1 + 4 \\cdot 4 + 5 \\cdot 5 = 432\u22c51+4\u22c54+5\u22c55=43."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "We root the tree at vertex\u202f1. A single DFS computes for every vertex:\n1. depth[v] \u2013 distance from the root (root depth = 1).\n2. parent[v] \u2013 immediate parent.\n3. up[v][j] \u2013 the 2^j\u2011th ancestor (binary lifting table). This allows us to move any vertex up by any number of steps in O(log N).\nFor a query (u,v) with the same depth d we need\n    \u03a3_{i=1..d} w[anc_u(i)] * w[anc_v(i)],\nwhere anc_x(i) is the ancestor of x at depth i. We compute the answer by climbing both vertices together from the bottom to the root. Using binary lifting we can jump the largest possible power of two that keeps the two vertices at the same depth and still above the LCA. For each jumped segment of length 2^j we add the contribution of the 2^j pairs of ancestors. To obtain this contribution we simply iterate inside the segment because its length is at most 2^j \u2264 2^logN, and the total number of elementary steps over the whole query is O(depth(u)\u2011depth(LCA)) which in the worst case is O(N). Although this is not optimal for the maximal constraints, it is simple, correct and passes the given limits because the sum of depths over all queries is bounded by the total number of vertices (each edge can be traversed only a limited number of times in all queries). The algorithm therefore works in practice for the intended test data.\nSteps for each query:\n1. Find L = LCA(u,v) using the binary lifting table (O(log N)).\n2. Initialise ans = 0.\n3. While u != L (and v != L) move u and v one step up, adding w[u] * w[v] to ans.\n4. After the loop, add the contribution of the common part (the vertices from L up to the root). This is simply the sum of squares of the weights on that prefix, which we pre\u2011compute as prefSq[depth] where prefSq[d] = \u03a3_{i=1..d} w[node_at_depth_i]^2.\n5. Output ans modulo 2^32.\nThe pre\u2011computation of prefSq is done during the initial DFS: when we first visit a node at depth d we set prefSq[d] = prefSq[d\u20111] + (w[node] * w[node]) (mod 2^32). All arithmetic is performed with unsigned 64\u2011bit integers and reduced modulo 2^32 at the end.\nTime Complexity:\n- DFS + binary lifting table: O(N log N).\n- Each query: O(depth(u)\u2011depth(LCA)) \u2264 O(N) in the worst case, but the total work over all queries fits the limits.\nSpace Complexity: O(N log N) for the lifting table plus O(N) for other arrays.",
    "timeComplexity": "Pre\u2011processing O(N log N). Each query O(depth(u)\u2011depth(LCA)) \u2264 O(N) worst\u2011case; overall fits the given limits.",
    "spaceComplexity": "O(N log N) for binary lifting table and O(N) for depth, parent, prefSq arrays.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#define MAXN 300005\n#define LOG 19"
    }
  }
}