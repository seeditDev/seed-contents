{
  "questionId": "Q1736",
  "title": "Merging 2 arrays",
  "slug": "merging-2-arrays",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-01",
  "metadata": {
    "category": "Arrays",
    "difficulty": "Easy",
    "isPremium": false,
    "tags": [
      "arrays",
      "data structures and algorithms"
    ],
    "companyTags": [],
    "topicTags": [
      "Arrays"
    ]
  },
  "content": {
    "problemStatement": "**Long Answer - 5 marks**\n\nMerging two 1D arrays involves combining the elements of both arrays into a single array while maintaining their original order.\n\nHere's a step-by-step process for merging two 1D arrays:\n\n\nCreate a New Array: Create a new array that is large enough to hold the combined elements of both arrays. The size of the new array should be the sum of the sizes of the two original arrays.\nCopy Elements: Iterate through the elements of the first array and copy them to the new array. Then, iterate through the elements of the second array and copy them to the new array after the elements of the first array.\nResult: The new array now contains all the elements from both original arrays, merged in the desired order.\nTaskYou are given a template code in the IDE. \n\nUpdate the code to merge the 2 arrays based on the process defined above.",
    "constraints": [],
    "inputFormat": "",
    "outputFormat": "",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n1 2 3\n4\n4 5 6 7",
        "output": "1 2 3 4 5 6 7",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "input": "3\n1 2 3\n4\n4 5 6 7",
        "expectedOutput": "1 2 3 4 5 6 7",
        "weight": 100
      }
    ],
    "totalTestCases": 1,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      100
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "",
    "timeComplexity": "O(N)",
    "spaceComplexity": "O(1)",
    "code": {
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main(){\n    int arr1[100] = {2, 4, 6}; // First array\n    int size1 = 3;\n\n    int arr2[100] = {8, 10, 12, 14}; // Second array\n    int size2 = 4;\n\n    // Solution as follows\n    int merged[200]; // Merged array\n    int mergedSize = size1 + size2; // Size of the merged array\n\n    // Copy elements from the first array to the merged array\n    for(int i=0 ; i<size1 ; i++) {\n        merged[i] = arr1[i];\n    }\n\n    // Copy elements from the second array to the merged array\n    for(int i=0 ; i<size2 ; i++) {\n        merged[size1+i] = arr2[i];\n    }\n    \n    // Print the merged array\n    for(int i=0 ; i<mergedSize ; i++){\n        cout<<merged[i]<<\" \";\n    }\n\n    return 0;\n}",
      "Python3": "# First array\narr1 = [2, 4, 6]\nsize1 = 3\n\n# Second array\narr2 = [8, 10, 12, 14]\nsize2 = 4\n\n# Merged array\nmerged = [0] * (size1 + size2)  # Initialize with zeroes\nmergedSize = size1 + size2\n\n# Copy elements from the first array to the merged array\nfor i in range(size1):\n    merged[i] = arr1[i]\n\n# Copy elements from the second array to the merged array\nfor i in range(size2):\n    merged[size1 + i] = arr2[i]\n\n# Print the merged array\nfor i in range(mergedSize):\n    print(merged[i], end=\" \")\n",
      "C": "#include <stdio.h>\n\nint main() {\n    int arr1[100] = {2, 4, 6}; // First array\n    int size1 = 3;\n\n    int arr2[100] = {8, 10, 12, 14}; // Second array\n    int size2 = 4;\n\n    int merged[200]; // Merged array\n    int mergedSize = size1 + size2; // Size of the merged array\n\n    // Copy elements from the first array to the merged array\n    for (int i = 0; i < size1; i++) {\n        merged[i] = arr1[i];\n    }\n\n    // Copy elements from the second array to the merged array\n    for (int i = 0; i < size2; i++) {\n        merged[size1 + i] = arr2[i];\n    }\n\n    // Print the merged array\n    for (int i = 0; i < mergedSize; i++) {\n        printf(\"%d \", merged[i]);\n    }\n\n    return 0;\n}\n",
      "Java": "public class Main {\n    public static void main(String[] args) {\n        int[] arr1 = {2, 4, 6}; // First array\n        int size1 = 3;\n\n        int[] arr2 = {8, 10, 12, 14}; // Second array\n        int size2 = 4;\n\n        int[] merged = new int[size1 + size2]; // Merged array\n        int mergedSize = size1 + size2; // Size of the merged array\n\n        // Copy elements from the first array to the merged array\n        for (int i = 0; i < size1; i++) {\n            merged[i] = arr1[i];\n        }\n\n        // Copy elements from the second array to the merged array\n        for (int i = 0; i < size2; i++) {\n            merged[size1 + i] = arr2[i];\n        }\n\n        // Print the merged array\n        for (int i = 0; i < mergedSize; i++) {\n            System.out.print(merged[i] + \" \");\n        }\n    }\n}\n",
      "JavaScript": "let arr1 = [2, 4, 6]; // First array\nlet size1 = 3;\n\nlet arr2 = [8, 10, 12, 14]; // Second array\nlet size2 = 4;\n\n// Solution as follows\nlet merged = []; // Merged array\nlet mergedSize = size1 + size2; // Size of the merged array\n\n// Copy elements from the first array to the merged array\nfor (let i = 0; i < size1; i++) {\n    merged[i] = arr1[i];\n}\n\n// Copy elements from the second array to the merged array\nfor (let i = 0; i < size2; i++) {\n    merged[size1 + i] = arr2[i];\n}\n\n// Print the merged array\nfor (let i = 0; i < mergedSize; i++) {\n    console.log(merged[i] + \" \");\n}\n"
    }
  }
}