{
  "questionId": "Q1374",
  "title": "Linear Search in array",
  "slug": "linear-search-in-array",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-01",
  "metadata": {
    "category": "Searching",
    "difficulty": "Easy",
    "isPremium": false,
    "tags": [
      "searching",
      "data structures and algorithms"
    ],
    "companyTags": [],
    "topicTags": [
      "Searching"
    ]
  },
  "content": {
    "problemStatement": "Write a program to search for a specific element in an array and print \"**Yes**\" if the element is present, otherwise print \"**No**\".\n\n\nInput Format\n\nThe first line contains an integer nnn, the length of the array and kkk, the element to be search.\nThe second line contains nnn space-separated integers representing the elements of the array.\n\n\nOutput Format\n\nPrint \"**Yes**\" if the element kkk is present in the array.\nPrint \"**No**\" if the element kkk is not present in the array.",
    "constraints": [],
    "inputFormat": "",
    "outputFormat": "",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "8 1\n3 5 1 4 5 6 5 6",
        "output": "Yes",
        "explanation": ""
      },
      {
        "id": "sample_2",
        "input": "3 4\n1 2 3",
        "output": "No",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "input": "8 1\n3 5 1 4 5 6 5 6",
        "expectedOutput": "Yes",
        "weight": 50
      },
      {
        "id": "tc_2",
        "input": "3 4\n1 2 3",
        "expectedOutput": "No",
        "weight": 50
      }
    ],
    "totalTestCases": 2,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      50,
      50
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved using a straightforward linear search algorithm.\n\n\nWe iterate through the array elements using a loop.\nFor each element in the array, we check if it matches the target element `k`.\nIf we find a match, we set a flag (`found`) to `true` and break out of the loop since we no longer need to check the remaining elements.\nIf the loop completes without finding the element, the flag remains `false`.\nAfter completing the search, we print “Yes” if the flag is `true`, indicating that the element was found.\nOtherwise, we print “No” if the flag is `false`, indicating that the element was not found.",
    "timeComplexity": "The time complexity of the solution is O(n), where n is the number of elements in the array. In the worst case, we may need to check every element of the array, which results in linear time complexity.",
    "spaceComplexity": "The space complexity is O(1) for the extra space used for variables like k, found, and the loop counter. The space used by the array itself is O(n), but this is considered part of the input rather than additional space used by the algorithm.",
    "code": {
      "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    int n, k;\n\n    // Read the length of the array\n    cin >> n;\n    \n    // Read the element to search for\n    cin >> k;\n\n    // Read the array elements\n    int array[n];\n    for (int i = 0; i < n; ++i) {\n        cin >> array[i];\n    }\n\n    // Initialize a flag to indicate if the element is found\n    bool found = false;\n\n    // Use a for loop to search for the element in the array\n    for (int i = 0; i < n; ++i) {\n        if (array[i] == k) {\n            found = true;\n            break;\n        }\n    }\n\n    // Print the result\n    if (found) {\n        cout << \"Yes\" << endl;\n    } else {\n        cout << \"No\" << endl;\n    }\n\n    return 0;\n}\n",
      "Python3": "# Read the input values\nn, k = map(int, input().split())\narray = list(map(int, input().split()))\n\n# Initialize a flag to indicate if the element is found\nfound = False\n\n# Use a for loop to search for the element in the array\nfor element in array:\n    if element == k:\n        found = True\n        break\n\n# Print the result\nif found:\n    print(\"Yes\")\nelse:\n    print(\"No\")\n",
      "C": "#include <stdio.h>\n#include <stdbool.h>\n\nint main() {\n    int n, k;\n\n    // Read the length of the array\n    scanf(\"%d\", &n);\n\n    // Read the element to search for\n    scanf(\"%d\", &k);\n\n    // Read the array elements\n    int array[n];\n    for (int i = 0; i < n; ++i) {\n        scanf(\"%d\", &array[i]);\n    }\n\n    // Initialize a flag to indicate if the element is found\n    bool found = false;\n\n    // Use a for loop to search for the element in the array\n    for (int i = 0; i < n; ++i) {\n        if (array[i] == k) {\n            found = true;\n            break;\n        }\n    }\n\n    // Print the result\n    if (found) {\n        printf(\"Yes\\n\");\n    } else {\n        printf(\"No\\n\");\n    }\n\n    return 0;\n}\n",
      "Java": "import java.util.Scanner;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n\n        // Read the length of the array\n        int n = scanner.nextInt();\n\n        // Read the element to search for\n        int k = scanner.nextInt();\n\n        // Read the array elements\n        int[] array = new int[n];\n        for (int i = 0; i < n; ++i) {\n            array[i] = scanner.nextInt();\n        }\n\n        // Initialize a flag to indicate if the element is found\n        boolean found = false;\n\n        // Use a for loop to search for the element in the array\n        for (int i = 0; i < n; ++i) {\n            if (array[i] == k) {\n                found = true;\n                break;\n            }\n        }\n\n        // Print the result\n        if (found) {\n            System.out.println(\"Yes\");\n        } else {\n            System.out.println(\"No\");\n        }\n    }\n}\n",
      "JavaScript": "const readline = require('readline');\n\nfunction main() {\n    const rl = readline.createInterface({\n        input: process.stdin,\n        output: process.stdout\n    });\n\n    let inputLines = [];\n\n    rl.on('line', (line) => {\n        inputLines.push(line);\n        if (inputLines.length === 2) {\n            rl.close();\n        }\n    });\n\n    rl.on('close', () => {\n        let [n, k] = inputLines[0].split(' ').map(Number);\n        let array = inputLines[1].split(' ').map(Number);\n        \n        let found = false;\n        \n        // Use a for loop to search for the element in the array\n        for (let i = 0; i < n; ++i) {\n            if (array[i] === k) {\n                found = true;\n                break;\n            }\n        }\n        \n        // Print the result using if-else\n        if (found) {\n            console.log(\"Yes\");\n        } else {\n            console.log(\"No\");\n        }\n    });\n}\n\n// Call the main function\nmain();\n"
    }
  }
}