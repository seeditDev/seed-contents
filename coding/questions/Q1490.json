{
  "questionId": "Q1490",
  "title": "Single number in multiple numbers",
  "slug": "single-number-in-multiple-numbers",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-01",
  "metadata": {
    "category": "Bit Manipulation",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "bit manipulation",
      "data structures and algorithms"
    ],
    "companyTags": [],
    "topicTags": [
      "Bit Manipulation"
    ]
  },
  "content": {
    "problemStatement": "You are given a **non-empty** array of integers numsnumsnums.\n\nIn this array, every number occurs **exactly twice** except for one number that occurs only once.\n\nYour task is to find and return that unique number.\n\nThe solution must run in **O(n)** time complexity and use **O(1)** space complexity.\n\n**Function Declaration****Function Name**singleNumbersingleNumbersingleNumber – Finds the one number in the array that appears exactly once while all other numbers appear twice.\n\n**Parameters**\nnumsnumsnums : A list/array of integers where every value appears exactly twice except one.\n**Return Value**\nReturns an **integer** — \n\nthe unique number that appears only once.\nConstraints:\n1≤nums.length≤3∗1041 \\leq nums.length \\leq 3 * 10^41≤nums.length≤3∗104\n−3∗104≤nums[i]≤3∗104-3 * 10^4 \\leq nums[i] \\leq 3 * 10^4−3∗104≤nums[i]≤3∗104\nExactly one element in the array appears once, and all others appear twice.\n\nInput Format\n\nNNN → number of elements in the array\nNext line → **N integers** representing numsnumsnums\n\n\nOutput Format\n\nPrint the single number that appears exactly once.",
    "constraints": [],
    "inputFormat": "",
    "outputFormat": "",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n1\n10\n5\n9 1 9 2 1\n5\n7 3 5 3 7",
        "output": "10\n2\n5",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "input": "3\n1\n10\n5\n9 1 9 2 1\n5\n7 3 5 3 7",
        "expectedOutput": "10\n2\n5",
        "weight": 100
      }
    ],
    "totalTestCases": 1,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      100
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Initialize a variable `result` to 0.\n\n\n\n\nIterate through the array:\n\n\n\nFor each element `num`, update `result = result XOR num`.\n\n\n\nAfter processing the entire array, `result` will contain the single number.\n\n\n\n**Why it works:**\n\n\nEvery element that appears twice will XOR to 0.\nThe element that appears once will remain because `0 XOR unique = unique`.\nComplexity Analysis\n\n**Time Complexity:** `O(n)`\n\n\n\nEach element is visited once.\n\n\n\n**Space Complexity:** `O(1)`\n\n\n\nOnly a single variable is used for XOR accumulation.\n\n\nExamples\n\n**Input:** `[2, 2, 1]`\n**XOR computation:** `0 ^ 2 ^ 2 ^ 1 = 1` → **Output:** `1`\n\n\n\n\n**Input:** `[4, 1, 2, 1, 2]`\n**XOR computation:** `0 ^ 4 ^ 1 ^ 2 ^ 1 ^ 2 = 4` → **Output:** `4`\n\n\n\n\n**Input:** `[1]`\n**XOR computation:** `0 ^ 1 = 1` → **Output:** `1`\n\n\n\nNotes / Edge Cases\nThe array will always have **exactly one unique element**, otherwise the XOR trick gives the XOR of all single occurrences.\nWorks for **negative numbers** as well because XOR operates at the bit level.\nOptimal for **large arrays** (up to `10^4`–`3*10^4`) due to its linear runtime and constant space.\nConclusion\nThe XOR method is elegant, concise, and optimal for this problem.\nIt leverages the **mathematical properties of XOR** rather than using extra memory structures like hash maps or sets.",
    "timeComplexity": "O(N)",
    "spaceComplexity": "O(1)",
    "code": {
      "C++": "class Solution {\npublic:\n    int singleNumber(vector<int>& nums) {\n        int result = 0;\n        for (int num : nums) {\n            result ^= num;  // XOR accumulates the unique number\n        }\n        return result;\n    }\n};",
      "Python3": "class Solution:\n    def singleNumber(self, nums):\n        result = 0\n        for num in nums:\n            result ^= num  # XOR accumulates the unique number\n        return result\n\n",
      "C": "int singleNumber(int nums[], int n) {\n    int result = 0;\n    for (int i = 0; i < n; i++) {\n        result ^= nums[i]; // XOR accumulates the unique number\n    }\n    return result;\n}",
      "Java": "class Solution {\n    public int singleNumber(int[] nums) {\n        int result = 0;\n        for (int num : nums) {\n            result ^= num; // XOR accumulates the unique number\n        }\n        return result;\n    }\n}",
      "JavaScript": "class Solution {\n    singleNumber(nums) {\n        let result = 0;\n        for (let num of nums) {\n            result ^= num; // XOR accumulates the unique number\n        }\n        return result;\n    }\n}\n\n"
    }
  }
}